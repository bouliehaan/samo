import { TFunction } from 'i18next';
export declare const normalizeReleaseTypes: (types: string[], t: TFunction) => string[];
export declare const normalizeToPrimaryReleaseTypes: (types: string[], t: TFunction) => string[];
export declare const normalizeToSecondaryReleaseTypes: (types: string[], t: TFunction) => string[];
