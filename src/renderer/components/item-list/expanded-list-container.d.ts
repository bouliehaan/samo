import { ReactNode } from 'react';
export interface ExpandedListContainerProps {
    children: ReactNode;
}
export declare const ExpandedListContainer: ({ children }: ExpandedListContainerProps) => import("react/jsx-runtime").JSX.Element;
