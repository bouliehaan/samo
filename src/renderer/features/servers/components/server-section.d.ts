import React from 'react';
interface ServerSectionProps {
    children: React.ReactNode;
    title: React.ReactNode | string;
}
export declare const ServerSection: ({ children, title }: ServerSectionProps) => import("react/jsx-runtime").JSX.Element;
export {};
