import { LibraryItem } from '/@/shared/types/domain-types';

interface SetRatingActionProps {
    ids: string[];
    itemType: LibraryItem;
}

export const SetRatingAction = (_props: SetRatingActionProps) => {
    void _props;
    return null;
};
