import type { ReactNode } from 'react';
interface TitlebarProps {
    children?: ReactNode;
}
export declare const Titlebar: ({ children }: TitlebarProps) => import("react/jsx-runtime").JSX.Element;
export {};
