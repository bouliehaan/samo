import { AppThemeConfiguration } from './app-theme-types';
import { AppTheme } from './app-theme-types';
export declare const appTheme: Record<AppTheme, AppThemeConfiguration>;
export declare const getAppTheme: (theme: AppTheme) => AppThemeConfiguration;
