extends: ./electron-builder.yml

# Nightly artifacts intentionally skip the appstream metainfo update.
afterAllArtifactBuild: null

publish:
    provider: s3
    bucket: samo-nightly
    channel: alpha
    endpoint: https://065f090c64de2dc707dd70ac72db9669.r2.cloudflarestorage.com
