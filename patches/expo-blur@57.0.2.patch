diff --git a/android/src/main/java/expo/modules/blur/BlurModule.kt b/android/src/main/java/expo/modules/blur/BlurModule.kt
index 837b7fe81001e2da88f6200ede58f8b8602f2a2a..10a4495fd5bbbf186ae92fe08d13da56258a2fa8 100644
--- a/android/src/main/java/expo/modules/blur/BlurModule.kt
+++ b/android/src/main/java/expo/modules/blur/BlurModule.kt
@@ -28,6 +28,21 @@ class BlurModule : Module() {
         view.applyBlurReduction(blurReductionFactor)
       }
 
+      // SAMO: chroma boost + multiplicative dim, applied inside the blur's own
+      // RenderEffect chain rather than as a React-layer overlay.
+      Prop("saturation") { view: ExpoBlurView, saturation: Float ->
+        view.setSaturation(saturation)
+      }
+
+      Prop("brightness") { view: ExpoBlurView, brightness: Float ->
+        view.setBrightness(brightness)
+      }
+
+      // SAMO: park the per-frame capture without unmounting the pane.
+      Prop("blurAutoUpdate") { view: ExpoBlurView, blurAutoUpdate: Boolean ->
+        view.setBlurAutoUpdate(blurAutoUpdate)
+      }
+
       Prop("blurMethod") { view: ExpoBlurView, blurMethod: BlurMethod ->
         view.setBlurMethod(blurMethod)
       }
diff --git a/android/src/main/java/expo/modules/blur/ExpoBlurView.kt b/android/src/main/java/expo/modules/blur/ExpoBlurView.kt
index 71344c8df533c25facea5fe7f5ed91528d15c1ed..2b0056431acf105a94663a9b371be1317959d53c 100644
--- a/android/src/main/java/expo/modules/blur/ExpoBlurView.kt
+++ b/android/src/main/java/expo/modules/blur/ExpoBlurView.kt
@@ -3,6 +3,9 @@ package expo.modules.blur
 import android.annotation.SuppressLint
 import android.content.Context
 import android.graphics.Color
+import android.graphics.ColorMatrix
+import android.graphics.ColorMatrixColorFilter
+import android.graphics.RenderEffect
 import android.os.Build
 import eightbitlab.com.blurview.BlurView
 import expo.modules.blur.enums.BlurMethod
@@ -10,6 +13,13 @@ import expo.modules.blur.enums.TintStyle
 import expo.modules.kotlin.AppContext
 import expo.modules.kotlin.views.ExpoView
 
+// SAMO: the snapshot downscale BlurView 3.x's one-argument `setupWith(target)`
+// applies. Mirrored here because the overload used below has no defaults.
+private const val DEFAULT_SCALE_FACTOR = 4f
+
+// SAMO: deliberately NOT the library default (true) — see configureBlurView.
+private const val APPLY_NOISE = false
+
 private enum class BlurViewConfiguration {
   // BlurView is yet to be configured.
   UNCONFIGURED,
@@ -31,11 +41,88 @@ class ExpoBlurView(context: Context, appContext: AppContext) : ExpoView(context,
   private var blurTargetId: Int? = null
   private var blurTarget: ExpoBlurTargetView? = null
 
+  // SAMO: chroma boost and multiplicative dim, applied to the blurred output.
+  //
+  // These are applied as a RenderEffect ON THE BLURVIEW ITSELF, not through a
+  // custom BlurAlgorithm, and that is load-bearing: BlurView 3.x's
+  // `setupWith(target, algorithm, scaleFactor, applyNoise)` only forwards the
+  // algorithm on the SOFTWARE path. When `BlurTarget.canUseHardwareRendering`
+  // (i.e. API >= 31) it builds a RenderNodeBlurController and DISCARDS the
+  // algorithm entirely — so a custom algorithm silently does nothing on every
+  // modern device, which is exactly how the dock lost its dim and started
+  // reading as light grey glass instead of black.
+  //
+  // A View-level RenderEffect composes over whatever the controller drew, so it
+  // works on both paths. The BlurView's only child is the blur itself; the
+  // smoke tint and the finish are React-side SIBLINGS of this view, so nothing
+  // else is caught by the transform.
+  private var saturation = 1f
+  private var brightness = 1f
+
+  // SAMO: whether this pane keeps re-capturing its target every frame. A
+  // BlurView's capture runs in an OnPreDrawListener, so a live pane costs a
+  // full redraw of its target on every frame the app produces — even when the
+  // pane is parked off-screen or sitting behind an opaque scrim. Turning it off
+  // leaves the view in place drawing its LAST captured frame, which is exactly
+  // what a frozen pane should look like.
+  private var blurAutoUpdate = true
+
   private val blurView = BlurView(context).also {
     it.layoutParams = LayoutParams(LayoutParams.MATCH_PARENT, LayoutParams.MATCH_PARENT)
     addView(it)
   }
 
+  fun setSaturation(value: Float) {
+    saturation = value
+    applyColorTransform()
+  }
+
+  fun setBrightness(value: Float) {
+    brightness = value
+    applyColorTransform()
+  }
+
+  /**
+   * Folds [saturation] and [brightness] into ONE ColorMatrix and hangs it on the
+   * BlurView as a RenderEffect. Uniform RGB scaling commutes with the saturation
+   * mix, so the order of the two is irrelevant.
+   *
+   * A multiplicative dim inside the effect darkens the backdrop while PRESERVING
+   * its contrast and chroma ratios — unlike piling alpha onto the React-side
+   * smoke tint, which veils the very content it is supposed to show through.
+   * That is what lets the dock read as near-black smoked glass and still look
+   * alive when bright artwork moves underneath it.
+   *
+   * RenderEffect is API 31+. Below that the blur is the RenderScript software
+   * path and simply goes untinted, which is what the pre-upgrade build did too.
+   */
+  private fun applyColorTransform() {
+    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
+      return
+    }
+    if (saturation == 1f && brightness == 1f) {
+      blurView.setRenderEffect(null)
+      return
+    }
+    val matrix = ColorMatrix().apply { setSaturation(saturation) }
+    if (brightness != 1f) {
+      matrix.postConcat(ColorMatrix().apply { setScale(brightness, brightness, brightness, 1f) })
+    }
+    blurView.setRenderEffect(
+      RenderEffect.createColorFilterEffect(ColorMatrixColorFilter(matrix))
+    )
+  }
+
+  fun setBlurAutoUpdate(enabled: Boolean) {
+    if (enabled == blurAutoUpdate) {
+      return
+    }
+    blurAutoUpdate = enabled
+    if (blurConfiguration == BlurViewConfiguration.DIMEZIS) {
+      blurView.setBlurAutoUpdate(enabled)
+    }
+  }
+
   fun setBlurTargetId(blurTargetId: Int?) {
     if (blurTargetId == this.blurTargetId) {
       return
@@ -159,10 +246,25 @@ class ExpoBlurView(context: Context, appContext: AppContext) : ExpoView(context,
     val dimezisBlurTarget = blurTarget?.blurTargetView
       ?: throw BlurViewConfigurationException("The BlurView targeting blur target with id: $blurTargetId couldn't find the target")
 
-    blurView.setupWith(dimezisBlurTarget)
+    // SAMO: `applyNoise` is FALSE. It is new in BlurView 3.x and defaults on;
+    // 2.0.6 — what this app's material was designed against — had no noise at
+    // all. Samo already lays its own pre-dithered film grain over the glass as
+    // part of the finish PNG, so the library's grain is both a second helping
+    // and a lightening one, which is part of why the dock started reading grey.
+    // setupWith returns a FACADE; the chained setters live there, not on
+    // BlurView. No algorithm is passed: on API 31+ BlurView discards it (see
+    // applyColorTransform), and below 31 this class's RenderEffect-based
+    // transform is unavailable anyway.
+    blurView.setupWith(dimezisBlurTarget, DEFAULT_SCALE_FACTOR, APPLY_NOISE)
       .setFrameClearDrawable(decorView.background)
       .setBlurRadius(blurRadius / blurReduction)
 
+    // The controller was just rebuilt, so re-hang the colour transform.
+    applyColorTransform()
+
+    // Replay the parked/live state onto the freshly configured view.
+    blurView.setBlurAutoUpdate(blurAutoUpdate)
+
     blurConfiguration = BlurViewConfiguration.DIMEZIS
   }
 
diff --git a/build/BlurView.js b/build/BlurView.js
index 427247c40ebd497523feb22db4ee085de825d07f..23af34ad5a0a4481ccdb64cfb0210986f96c03e2 100644
--- a/build/BlurView.js
+++ b/build/BlurView.js
@@ -54,8 +54,8 @@ export default class BlurView extends React.Component {
         return providedMethod ?? 'none';
     }
     render() {
-        const { tint = 'default', intensity = 50, blurReductionFactor = 4, style, children, ...props } = this.props;
-        return (_jsxs(View, { ...props, style: [styles.container, style], children: [_jsx(NativeBlurView, { blurTargetId: this.state.blurTargetId, ref: this.blurViewRef, tint: tint, intensity: intensity, blurReductionFactor: blurReductionFactor, blurMethod: this._getBlurMethod(), style: StyleSheet.absoluteFill }), children] }));
+        const { tint = 'default', intensity = 50, blurReductionFactor = 4, saturation = 1, brightness = 1, blurAutoUpdate = true, style, children, ...props } = this.props;
+        return (_jsxs(View, { ...props, style: [styles.container, style], children: [_jsx(NativeBlurView, { blurTargetId: this.state.blurTargetId, ref: this.blurViewRef, tint: tint, intensity: intensity, blurReductionFactor: blurReductionFactor, saturation: saturation, brightness: brightness, blurAutoUpdate: blurAutoUpdate, blurMethod: this._getBlurMethod(), style: StyleSheet.absoluteFill }), children] }));
     }
 }
 const styles = StyleSheet.create({
diff --git a/build/BlurView.types.d.ts b/build/BlurView.types.d.ts
index 0b48f682f4a32dd1751b71480ddd723ea3cad1e9..038ec32175a93c5ed314fe5e57f61c9ed23c4bfb 100644
--- a/build/BlurView.types.d.ts
+++ b/build/BlurView.types.d.ts
@@ -63,6 +63,25 @@ export type BlurViewProps = {
      * @platform android
      */
     blurMethod?: BlurMethod;
+    /**
+     * Chroma boost applied INSIDE the blur's own RenderEffect chain (1 = unchanged).
+     * @platform android
+     */
+    saturation?: number;
+    /**
+     * Uniform RGB scale applied inside the same chain as `saturation` (1 = unchanged).
+     * A multiplicative dim preserves the backdrop's contrast and chroma ratios,
+     * unlike stacking alpha on a tint overlay.
+     * @platform android
+     */
+    brightness?: number;
+    /**
+     * Whether the pane keeps re-capturing its target every frame. `false` parks
+     * the capture and leaves the last captured frame on screen.
+     * @default true
+     * @platform android
+     */
+    blurAutoUpdate?: boolean;
 } & ViewProps;
 export type BlurTargetViewProps = {
     ref?: RefObject<View | null>;
diff --git a/src/BlurView.tsx b/src/BlurView.tsx
index b37ba3365db10ffb931040ae4080a2840dbf939e..3961fb06fd4a5bd098b71c6b19a2a119739d4e93 100644
--- a/src/BlurView.tsx
+++ b/src/BlurView.tsx
@@ -80,6 +80,9 @@ export default class BlurView extends React.Component<BlurViewProps, BlurViewSta
       tint = 'default',
       intensity = 50,
       blurReductionFactor = 4,
+      saturation = 1,
+      brightness = 1,
+      blurAutoUpdate = true,
       style,
       children,
       ...props
@@ -92,6 +95,9 @@ export default class BlurView extends React.Component<BlurViewProps, BlurViewSta
           tint={tint}
           intensity={intensity}
           blurReductionFactor={blurReductionFactor}
+          saturation={saturation}
+          brightness={brightness}
+          blurAutoUpdate={blurAutoUpdate}
           blurMethod={this._getBlurMethod()}
           style={StyleSheet.absoluteFill}
         />
diff --git a/src/BlurView.types.ts b/src/BlurView.types.ts
index b80d44aac0da6fc2829f3bccd87b0a8efea2d79b..8279584094f4011bb9255ae3758a62e82b4cd2fb 100644
--- a/src/BlurView.types.ts
+++ b/src/BlurView.types.ts
@@ -68,6 +68,23 @@ export type BlurViewProps = {
    * @platform android
    */
   blurMethod?: BlurMethod;
+  /**
+   * Chroma boost applied INSIDE the blur's own RenderEffect chain (1 = unchanged).
+   * @platform android
+   */
+  saturation?: number;
+  /**
+   * Uniform RGB scale applied inside the same chain as `saturation` (1 = unchanged).
+   * @platform android
+   */
+  brightness?: number;
+  /**
+   * Whether the pane keeps re-capturing its target every frame. `false` parks
+   * the capture and leaves the last captured frame on screen.
+   * @default true
+   * @platform android
+   */
+  blurAutoUpdate?: boolean;
 } & ViewProps;
 
 export type BlurTargetViewProps = {
